# Scenario: high TTFT (p95 > 2s), stable TPOT — prefill bottleneck.
# ttft_p95_seconds requires a Prometheus client; ScrapeClient always returns None.
# TYPE vllm:num_requests_waiting gauge
vllm:num_requests_waiting{engine="0",model_name="meta-llama/Llama-3.1-8B"} 10.0
